namespace BleizEntertainment
{
    /*public class BEAnimator : MonoBehaviour
    {
        [SerializeField]PlayerAnimationEventTrigger assignedTrigger;
        [SerializeField] public Animator animator;
        public void Init(RuntimeAnimatorController animatorController,CharacterHandler characterHandler, int assignedNumber)
        {
            animator = GetComponent<Animator>();
            animator.runtimeAnimatorController = animatorController;
            assignedTrigger = this.GetComponent<PlayerAnimationEventTrigger>();
        }
        void OnDestroy()
        {
            Destroy(assignedTrigger);
        }
    }*/
}
